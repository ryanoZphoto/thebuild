[build]
  command = "node scripts/generate-gallery.js && node scripts/generate-merchant-feed.js && echo 'Build complete'"

[dev]
  command = "node scripts/generate-gallery.js && node scripts/generate-merchant-feed.js"  # For local dev
