[build]
  command = "node scripts/generate-gallery.js && echo 'Build complete'"  # Add your existing build command if any, e.g., "&& npm run build"

[dev]
  command = "node scripts/generate-gallery.js"  # For local dev
